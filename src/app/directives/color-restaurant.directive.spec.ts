import { ColorRestaurantDirective } from './color-restaurant.directive';

describe('ColorRestaurantDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorRestaurantDirective();
    expect(directive).toBeTruthy();
  });
});
